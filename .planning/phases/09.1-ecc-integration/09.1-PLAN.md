---
phase: 09.1-ecc-integration
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
parent_phase: "09"
files_modified:
  - ~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts
  - ~/.claude/scientific-skills/skills/scientific-do/skill-router.ts
  - ~/.claude/scientific-skills/skills/scientific-do/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "intent-parser.ts Intent 接口增加 domain 字段: 'scientific' | 'engineering' | 'general' | 'mixed'"
    - "detectDomain() 通过科研关键词白名单判断输入是否属于科学域"
    - "skill-router.ts 新增 FALLBACK_GUIDE 常量，非 scientific 域导向 everything-claude-code"
    - "SKILL.md 新增能力边界声明和 everything-claude-code 交叉引用"
    - "现有 7-skill 科学路由逻辑不变"
    - "不维护 everything-claude-code 的完整路由表"
  artifacts:
    - path: ~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts
      provides: "intent detection with domain field"
      min_lines: 100
      contains: "detectDomain|Domain|scientific|engineering|general"
    - path: ~/.claude/scientific-skills/skills/scientific-do/skill-router.ts
      provides: "skill routing with fallback guide"
      min_lines: 100
      contains: "FALLBACK_GUIDE|everything-claude-code"
    - path: ~/.claude/scientific-skills/skills/scientific-do/SKILL.md
      provides: "skill documentation with capability boundaries"
      min_lines: 230
      contains: "Does NOT Handle|everything-claude-code"
---

<objective>
everything-claude-code 域识别路由集成：让 scientific-do 识别非科学请求并引导到 everything-claude-code，不合并两套 skill 体系。
</objective>

<context>
@.planning/todos/pending/2026-05-12-scientific-do-everything-claude-code.md
@C:\Users\Administrator.DESKTOP-BD1JUD0\.claude\plans\sharded-weaving-narwhal.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 intent-parser.ts — 增加 Domain 类型和 detectDomain()</name>
  <files>~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts</files>
  <read_first>
    - C:\Users\Administrator.DESKTOP-BD1JUD0\.claude\scientific-skills\skills\scientific-do\intent-parser.ts
  </read_first>
  <action>
    1. Intent 接口增加 `domain?: 'scientific' | 'engineering' | 'general' | 'mixed'`
    2. 新增 `detectDomain()` 函数：
       - 科学关键词白名单：文献、论文、研究、分析、统计、医学、临床、生物、基因、蛋白、药物、分子、细胞、流行病学、PRISMA、STROBE、CONSORT、meta分析、系统综述、RCT、队列、病例对照、诊断、预后、综述、润色、图表、绘图、投稿、发表
       - 工程关键词白名单：API、FastAPI、Django、React、Vue、Docker、K8s、deploy、security audit、test、TDD、database、SQL、NoSQL、CI/CD
       - 无匹配 → 'general'，同时含科学+工程 → 'mixed'，仅科学 → 'scientific'，仅工程 → 'engineering'
    3. parseIntent() 返回前调用 detectDomain()，附加 domain 到结果
    4. 现有 detectStage/detectTaskTypes/detectDomains/calculateConfidence 不变
  </action>
  <verify>grep -q "detectDomain\|domain.*scientific" intent-parser.ts</verify>
  <acceptance_criteria>
    - Intent 接口含 domain 字段
    - detectDomain() 存在且通过关键词判断
    - 科学输入 → domain='scientific'
    - 工程输入 → domain='engineering' or 'general'
    - 混合输入 → domain='mixed'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 skill-router.ts — 增加 FALLBACK_GUIDE 和域路由</name>
  <files>~/.claude/scientific-skills/skills/scientific-do/skill-router.ts</files>
  <read_first>
    - C:\Users\Administrator.DESKTOP-BD1JUD0\.claude\scientific-skills\skills\scientific-do\skill-router.ts (after Task 1)
  </read_first>
  <action>
    1. 新增 FALLBACK_GUIDE 常量：
       ```
       const FALLBACK_GUIDE = {
         everything_claude_code: 'everything-claude-code (通用开发增强: 200+ skills, 覆盖 FastAPI/Django/React/Docker/安全审计等)',
         exa_search: 'exa-search (网络搜索, 适合实时信息和新闻查询)',
         general_note: '当前请求不在 scientific-do 的科研能力范围内。请用 /clear 后直接向 Claude Code 提出，或使用 everything-claude-code 的通用开发 skills。'
       };
       ```
    2. routeSkill() 函数开头插入域检查：
       - domain === 'engineering' 或 domain === 'general' → 返回引导结果（primarySkill: 'FALLBACK'，含 FALLBACK_GUIDE 信息）
       - domain === 'scientific' 或 undefined → 走现有3-tier路由（不变）
    3. 新路由流程: parseIntent → detectDomain → general/engineering? → fallback / scientific? → 现有路由
  </action>
  <verify>grep -q "FALLBACK_GUIDE\|everything-claude-code" skill-router.ts</verify>
  <acceptance_criteria>
    - FALLBACK_GUIDE 常量存在
    - routeSkill() 在现有路由前检查 domain
    - general/engineering 域返回引导结果
    - scientific 域走现有路由不变
    - buildDependencyChain() 不变
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 更新 SKILL.md — 增加能力边界声明</name>
  <files>~/.claude/scientific-skills/skills/scientific-do/SKILL.md</files>
  <read_first>
    - C:\Users\Administrator.DESKTOP-BD1JUD0\.claude\scientific-skills\skills\scientific-do\SKILL.md (current state)
  </read_first>
  <action>
    在 "## When to Use" 节后（"## Process" 前）插入新节：
    
    ```
    ## What This Does NOT Handle
    
    scientific-do 专注于科研工作流（文献检索、数据分析、论文写作、图表生成、投稿润色、医学研究）。
    
    以下请求不在能力范围内，请直接使用 Claude Code 或 everything-claude-code：
    
    - 软件开发任务（写 API、前端页面、数据库设计、部署配置）
    - 安全审计与渗透测试
    - 通用 DevOps / CI/CD 配置
    - 非科研类的通用问答
    
    **everything-claude-code 集成:** 200+ 通用开发 skills 已安装于 `~/.claude/skills/everything-claude-code/`，覆盖 FastAPI、Django、React、Docker、安全审计等场景。当 scientific-do 检测到非科学请求时，会自动引导到 everything-claude-code。
    ```
    
    Do NOT modify frontmatter or any other section.
  </action>
  <verify>grep -q "Does NOT Handle\|everything-claude-code" SKILL.md</verify>
  <acceptance_criteria>
    - "What This Does NOT Handle" 节存在
    - 明确列出不在范围内的请求类型
    - 包含 everything-claude-code 交叉引用
    - 现有节（Process, Integration, When to Use）完整保留
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: 验证 — 回归测试 + 新功能测试</name>
  <files>
    ~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts
    ~/.claude/scientific-skills/skills/scientific-do/skill-router.ts
  </files>
  <action>
    验证内容:
    
    1. TypeScript 编译检查:
       npx tsc --noEmit intent-parser.ts skill-router.ts 2>/dev/null || node -e "require('fs').readFileSync('intent-parser.ts','utf8'); require('fs').readFileSync('skill-router.ts','utf8'); console.log('Syntax OK')"
    
    2. 手动逻辑验证:
       - 科学请求 "搜索COVID疫苗文献" → parseIntent 返回 domain='scientific'
       - 工程请求 "写一个FastAPI接口" → parseIntent 返回 domain 非 'scientific'
       - 科学请求仍正确路由到 deepxiv_sdk 等科研 skill
       - 工程请求路由到 FALLBACK，含 everything-claude-code 引导
    
    3. 确认 SKILL.md 完整性:
       - 检查 frontmatter 完整
       - 检查 Step 1-5 各节完整
       - 检查 Integration 节完整
    
    Fix any issues found.
  </action>
  <verify>node -e "
const fs=require('fs');
const ip=fs.readFileSync('intent-parser.ts','utf8');
const sr=fs.readFileSync('skill-router.ts','utf8');
const md=fs.readFileSync('SKILL.md','utf8');
const checks=[
  ip.includes('detectDomain'),
  ip.includes('domain'),
  ip.includes('scientific'),
  ip.includes('general'),
  sr.includes('FALLBACK_GUIDE'),
  sr.includes('everything-claude-code'),
  md.includes('Does NOT Handle'),
  md.includes('everything-claude-code')
];
console.log(checks.every(Boolean)?'ALL PASS':'FAIL: '+checks.map((c,i)=>c?'':['detectDomain','domain','scientific','general','FALLBACK_GUIDE','ecc-in-router','does-not-handle','ecc-in-skillmd'][i]).filter(Boolean).join(', '))" 2>&1
</verify>
  <acceptance_criteria>
    - TypeScript 语法无错误
    - 科学请求仍正确路由到科研 skill（回归）
    - 工程请求返回 FALLBACK 引导（新功能）
    - SKILL.md 完整 + 新增能力边界声明
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- [ ] intent-parser.ts: domain 字段 + detectDomain() 函数
- [ ] skill-router.ts: FALLBACK_GUIDE + 域路由逻辑
- [ ] SKILL.md: 能力边界声明 + everything-claude-code 引用
- [ ] 现有科学路由不受影响
- [ ] 所有文件语法正确
</success_criteria>

<output>
After completion, create .planning/phases/09.1-ecc-integration/09.1-SUMMARY.md
</output>
