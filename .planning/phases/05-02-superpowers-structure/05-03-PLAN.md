---
phase: "05"
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - "~/.claude/scientific-skills/skills/scientific-do/SKILL.md"
  - "~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts"
  - "~/.claude/scientific-skills/skills/scientific-do/skill-router.ts"
autonomous: true
requirements:
  - P5-03
  - P5-04
user_setup: []

must_haves:
  truths:
    - "scientific-do协调器具备意图解析能力"
    - "scientific-do协调器具备skill路由能力"
    - "scientific-do协调器具备冲突处理能力"
  artifacts:
    - path: "~/.claude/scientific-skills/skills/scientific-do/SKILL.md"
      provides: "协调器主文件（Per D-16）"
    - path: "~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts"
      provides: "意图解析模块"
    - path: "~/.claude/scientific-skills/skills/scientific-do/skill-router.ts"
      provides: "Skill路由模块"
  key_links:
    - from: "scientific-do"
      to: "7个学术Skill"
      via: "skill路由"
    - from: "scientific-do"
      to: "用户输入"
      via: "意图解析"
---

<objective>
制作scientific-do协调器，具备意图解析、skill路由、依赖链编排、冲突处理能力
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-02-superpowers-structure/05-CONTEXT.md

## Scientific-Do协调器设计（Per D-16, D-17, D-18, D-19）

### 核心功能
1. **意图解析**：解析用户输入，识别科研意图
2. **Skill路由**：根据意图选择合适的skill（Per D-17）
3. **依赖链编排**：确定skill执行顺序
4. **冲突处理**：解决多skill冲突（Per D-18）

### 触发优先级逻辑（Per D-17）
1. 精确匹配：Skill description与用户输入完全匹配
2. 模糊fallback：部分关键词匹配
3. 智能调优：根据上下文、用户偏好、历史使用习惯调整

### 冲突处理策略（Per D-18）
- 上下文优先级：当前任务上下文
- 用户偏好：用户在配置中的偏好设置
- 历史使用习惯：基于使用频率的推荐

### 状态管理（Per D-19）
- 全局状态：所有skill可读写
- 维护：skill执行状态、用户偏好、历史记录

### 触发条件示例
```yaml
---
name: scientific-do
description: "Use when starting a research task, making research decisions, or needing to coordinate multiple research skills"
---
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建scientific-do SKILL.md主文件</name>
  <files>"~/.claude/scientific-skills/skills/scientific-do/SKILL.md"</files>
  <action>
    创建scientific-do协调器的主文件 SKILL.md：

    ```yaml
    ---
    name: scientific-do
    description: "Use when starting a research task, making research decisions, or needing to coordinate multiple research skills. Central coordinator for the scientific-skills bundle."
    ---

    # Scientific-Do: Central Coordinator

    ## Overview
    Scientific-Do是scientific-skills集合的中央协调器，负责：
    - 意图解析：识别用户科研意图
    - Skill路由：选择合适的skill
    - 依赖链编排：确定执行顺序
    - 冲突处理：解决多skill冲突

    ## When to Use
    - 用户描述一个科研任务时
    - 需要多个skill协作时
    - 遇到skill选择冲突时

    ## Process

    ### 1. 意图解析
    解析用户输入，提取：
    - 科研阶段（文献/分析/写作/润色）
    - 任务类型（检索/分析/可视化/写作）
    - 领域关键词（医学/统计/机器学习）

    ### 2. Skill路由（Per D-17）
    按优先级选择skill：
    1. 精确匹配：description完全匹配
    2. 模糊fallback：关键词部分匹配
    3. 智能调优：上下文+偏好+历史

    ### 3. 依赖链编排
    确定执行顺序：
    - 文献检索 → 分析设计 → 执行分析 → 论文写作 → 图表生成 → 润色投稿
    - HARD-GATE：规划前必须研究、写作前必须设计、执行前必须确认

    ### 4. 冲突处理（Per D-18）
    当多个skill竞争时：
    - 上下文优先级：当前任务上下文
    - 用户偏好：用户配置文件中的设置
    - 历史使用习惯：使用频率统计

    ## Integration
    **Requires skills:**
    - deepxiv_sdk
    - academic-paper-analysis
    - scientific-agent-skills
    - academic-writing-skills
    - paper-plot-skills
    - Paper-Polish-Workflow-skill
    - medsci-skills

    **HARD-GATE nodes:**
    - 规划前必须研究
    - 写作前必须设计
    - 执行前必须确认
    ```
  </action>
  <verify>
    <automated>test -f ~/.claude/scientific-skills/skills/scientific-do/SKILL.md && grep -c "scientific-do" ~/.claude/scientific-skills/skills/scientific-do/SKILL.md</automated>
  </verify>
  <done>SKILL.md包含协调器完整定义</done>
</task>

<task type="auto">
  <name>Task 2: 创建意图解析模块</name>
  <files>"~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts"</files>
  <action>
    创建意图解析模块 intent-parser.ts：

    ```typescript
    // Intent Parser for Scientific-Do
    // Per D-16: 意图解析能力

    export interface Intent {
      stage: 'literature' | 'analysis' | 'writing' | 'polishing' | 'submission';
      taskType: string[];
      domainKeywords: string[];
      confidence: number;
    }

    export function parseIntent(input: string): Intent {
      // 1. 识别科研阶段
      const stages = {
        literature: ['文献', '检索', '搜索', 'paper', 'paper', 'article'],
        analysis: ['分析', '统计', '建模', 'analyze', 'analysis'],
        writing: ['写作', '写', '撰写', 'write', 'writing'],
        polishing: ['润色', '修改', 'polish'],
        submission: ['投稿', 'submit', 'submission']
      };

      // 2. 识别任务类型
      const taskTypes = {
        '文献检索': ['检索', '搜索', 'find'],
        '数据分析': ['分析', '统计', 'model'],
        '图表生成': ['图', '表', 'plot', 'chart'],
        '论文写作': ['写', '写作', 'write'],
        '论文润色': ['润色', 'polish', 'edit']
      };

      // 3. 识别领域关键词
      const domains = ['医学', 'biomedical', 'clinical', '统计', 'statistical'];

      // 返回解析结果
      return {
        stage: detectStage(input, stages),
        taskType: detectTaskTypes(input, taskTypes),
        domainKeywords: detectDomains(input, domains),
        confidence: calculateConfidence(input)
      };
    }

    function detectStage(input: string, stages: object): string { /* implementation */ }
    function detectTaskTypes(input: string, taskTypes: object): string[] { /* implementation */ }
    function detectDomains(input: string, domains: string[]): string[] { /* implementation */ }
    function calculateConfidence(input: string): number { /* implementation */ }
    ```
  </action>
  <verify>
    <automated>test -f ~/.claude/scientific-skills/skills/scientific-do/intent-parser.ts</automated>
  </verify>
  <done>意图解析模块创建完成</done>
</task>

<task type="auto">
  <name>Task 3: 创建Skill路由模块</name>
  <files>"~/.claude/scientific-skills/skills/scientific-do/skill-router.ts"</files>
  <action>
    创建Skill路由模块 skill-router.ts：

    ```typescript
    // Skill Router for Scientific-Do
    // Per D-16, D-17: Skill路由能力

    import { Intent } from './intent-parser';

    export interface RouteResult {
      primarySkill: string;
      secondarySkills: string[];
      executionOrder: string[];
      confidence: number;
    }

    // Skill映射表
    const SKILL_MAP = {
      '文献检索': ['deepxiv_sdk'],
      '论文分析': ['academic-paper-analysis', 'deepxiv_sdk'],
      '数据分析': ['scientific-agent-skills'],
      '论文写作': ['academic-writing-skills'],
      '图表生成': ['paper-plot-skills'],
      '投稿润色': ['Paper-Polish-Workflow-skill'],
      '医学专项': ['medsci-skills']
    };

    export function routeSkill(intent: Intent): RouteResult {
      // 1. 精确匹配
      const exactMatch = findExactMatch(intent);
      if (exactMatch) return exactMatch;

      // 2. 模糊fallback
      const fuzzyMatch = findFuzzyMatch(intent);
      if (fuzzyMatch) return fuzzyMatch;

      // 3. 智能调优
      return smartTuning(intent);
    }

    function findExactMatch(intent: Intent): RouteResult | null {
      // 根据intent查找精确匹配的skill
    }

    function findFuzzyMatch(intent: Intent): RouteResult | null {
      // 根据关键词模糊匹配skill
    }

    function smartTuning(intent: Intent): RouteResult {
      // 根据上下文、偏好、历史调整
      // Per D-18: 综合决策
    }

    // 依赖链编排
    export function buildDependencyChain(skills: string[]): string[] {
      const order = ['deepxiv_sdk']; // 文献优先

      if (skills.includes('scientific-agent-skills')) {
        order.push('scientific-agent-skills');
      }

      if (skills.includes('academic-writing-skills')) {
        order.push('academic-writing-skills');
      }

      if (skills.includes('paper-plot-skills')) {
        order.push('paper-plot-skills');
      }

      if (skills.includes('Paper-Polish-Workflow-skill')) {
        order.push('Paper-Polish-Workflow-skill');
      }

      return order;
    }
    ```
  </action>
  <verify>
    <automated>test -f ~/.claude/scientific-skills/skills/scientific-do/skill-router.ts</automated>
  </verify>
  <done>Skill路由模块创建完成</done>
</task>

</tasks>

<verification>
- scientific-do/SKILL.md存在且包含完整定义
- intent-parser.ts实现意图解析逻辑
- skill-router.ts实现路由和依赖链编排
</verification>

<success_criteria>
- 协调器具备意图解析、skill路由、冲突处理能力
- 符合D-16、D-17、D-18、D-19设计要求
</success_criteria>

<output>
After completion, create `.planning/phases/05-02-superpowers-structure/05-03-SUMMARY.md`
</output>
