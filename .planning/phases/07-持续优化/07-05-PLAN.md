---
phase: 07
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - "~/.claude/scientific-skills/skills/scientific-do/SKILL.md"
  - ".planning/phases/07-持续优化/benchmark-results.tsv"
  - ".planning/phases/07-持续优化/07-VERIFY.sh"
autonomous: false
requirements: [D-07, D-09]
user_setup: []

must_haves:
  truths:
    - "Scientific-do can detect skill gaps during task execution (D-09)"
    - "Benchmark results exist for all 7 core skills (D-07)"
    - "07-VERIFY.sh validates gap detection logic"
  artifacts:
    - path: "~/.claude/scientific-skills/skills/scientific-do/SKILL.md"
      provides: "Coordinator with gap detection node"
      contains: "Gap Detection"
    - path: ".planning/phases/07-持续优化/benchmark-results.tsv"
      provides: "Performance baseline for all 7 core skills"
      min_lines: 8
    - path: ".planning/phases/07-持续优化/07-VERIFY.sh"
      provides: "Updated verify script with D-09 check"
      contains: "Gap Detection"
  key_links:
    - from: "scientific-do Step 5 gap detection"
      to: "scripts/skill-discovery.sh"
      via: "When gap detected, triggers discovery search"
    - from: "benchmark-results.tsv"
      to: "future comparison runs"
      via: "Baseline data for measuring performance regression"
---

<objective>
Add skill gap detection to scientific-do, execute performance benchmark for P6-E01, and finalize all verification checks.

Purpose: D-09 requires scientific-do to automatically detect when no skill matches a user's request (skill gap) and trigger discovery. D-07 (P6-E01) requires establishing a performance baseline. This plan also adds the checkpoint for smoke test verification (D-18).
Output: Gap detection logic in scientific-do, benchmark results, updated 07-VERIFY.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-持续优化/07-CONTEXT.md
@.planning/phases/07-持续优化/07-RESEARCH.md
@~/.claude/scientific-skills/skills/scientific-do/SKILL.md
@.planning/phases/07-持续优化/scripts/benchmark.sh
@.planning/phases/07-持续优化/scripts/skill-discovery.sh
@.planning/phases/07-持续优化/07-VERIFY.sh
@.planning/phases/06-测试执行/06-04-ISSUES.md
</context>

<interfaces>
Current scientific-do Step 5 (from Plan 03):
```
### 5. Post-Action Verification Closure (D-05)
- Verification gates (4 gates)
- Usage Tracking (D-14, D-15)
- Counter trigger at every 10
```

scientific-skills-config.json skill_registry keys for gap detection reference:
```
deepxiv_sdk, academic-paper-analysis, scientific-agent-skills, academic-writing-skills,
paper-plot-skills, Paper-Polish-Workflow-skill, medsci-skills,
nature-skills, claude-scholar, scientify
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add gap detection node to scientific-do</name>
  <files>~/.claude/scientific-skills/skills/scientific-do/SKILL.md</files>

  <read_first>
    - Current Step 2 (Skill Routing) — to understand where gap detection fits
    - Current Step 5 (Post-Action Verification) — to add gap detection as a sub-step
    - scientific-skills-config.json skill_registry — to know which roles are covered
  </read_first>

  <action>
    Add a gap detection node at the end of Step 2 (Skill Routing), after the existing matching logic:

    ```markdown
    **Gap Detection (D-09):**

    If all routing stages (1-4) fail to match any skill with score >= 1:
    1. Log the gap: Record the user's request, context, and attempted matches
    2. Notify user: "Current skill bundle has no skill that matches this task."
    3. Offer discovery: "Would you like to search GitHub for candidate skills?"
       - If user confirms: Run `bash .planning/phases/07-持续优化/scripts/skill-discovery.sh`
       - Discovery script results provide candidate repos for Phase 2 evaluation
    4. Record gap in feedback-state.json under a `gaps` array:
       ```json
       {
         "gaps": [
           {
             "timestamp": "...",
             "request": "user request summary",
             "stage": "research/analysis/writing/polishing",
             "attempted_skills": ["skill1", "skill2"]
           }
         ]
       }
       ```
    ```

    Add the `gaps` array to `feedback-state.json` initial structure. Append `"gaps": []` to the file (after the `"version": 1` line). Use `jq` to add the field:
    ```bash
    jq '. + {"gaps": []}' ~/.claude/scientific-skills/feedback-state.json > tmp && mv tmp ~/.claude/scientific-skills/feedback-state.json
    ```

    **Note:** This only needs to run once (during implementation). If the gaps array already exists, do not duplicate it.
  </action>

  <verify>
    <automated>
      grep -q "Gap Detection (D-09)" ~/.claude/scientific-skills/skills/scientific-do/SKILL.md && \
      jq -e '.gaps | type == "array"' ~/.claude/scientific-skills/feedback-state.json > /dev/null && \
      echo "D-09 gap detection: OK" || echo "FAIL"
    </automated>
  </verify>

  <acceptance_criteria>
    - scientific-do/SKILL.md contains "Gap Detection (D-09)" section in Step 2
    - Gap detection has 4 sub-steps: log, notify, offer discovery, record gap
    - Offer discovery step references skill-discovery.sh
    - feedback-state.json has `"gaps": []` array
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Execute benchmark.sh and save results</name>
  <files>.planning/phases/07-持续优化/benchmark-results.tsv</files>

  <read_first>
    - `.planning/phases/07-持续优化/scripts/benchmark.sh` — verify it exists and understands expected output format
  </read_first>

  <action>
    Run the benchmark script and save its results:

    1. Execute:
    ```bash
    bash .planning/phases/07-持续优化/scripts/benchmark.sh
    ```

    2. If the script produces a TSV file, verify it contains 7 skill rows. If not, manually capture output.

    3. Write the results to `.planning/phases/07-持续优化/benchmark-results.tsv` with the following format:
    ```tsv
    # Phase 7 Performance Benchmark
    # Date: 2026-05-12
    # Command: bash scripts/benchmark.sh
    # Baseline for P6-E01
    Skill	Parse(ms)	Route(ms)	FileSize(bytes)
    deepxiv_sdk	{value}	{value}	{value}
    academic-paper-analysis	{value}	{value}	{value}
    scientific-agent-skills	{value}	{value}	{value}
    academic-writing-skills	{value}	{value}	{value}
    paper-plot-skills	{value}	{value}	{value}
    Paper-Polish-Workflow-skill	{value}	{value}	{value}
    medsci-skills	{value}	{value}	{value}
    ```

    4. Append a summary section at the bottom:
    ```
    # Summary
    # Average parse time: {avg}ms
    # Average route time: {avg}ms
    # Total skills: 7
    ```

    **IMPORTANT:** Fill in actual measured values from the benchmark run. Do not substitute placeholder values — run the script and capture real output.
  </action>

  <verify>
    <automated>
      test -f .planning/phases/07-持续优化/benchmark-results.tsv && \
      wc -l < .planning/phases/07-持续优化/benchmark-results.tsv | xargs test 8 -le && \
      head -3 .planning/phases/07-持续优化/benchmark-results.tsv | grep -q "Benchmark" && \
      echo "D-07 benchmark: OK" || echo "FAIL"
    </automated>
  </verify>

  <acceptance_criteria>
    - benchmark-results.tsv exists with at least 8 lines (1 header + 1 comment + 7 skill rows)
    - File contains metric columns: Skill, Parse(ms), Route(ms), FileSize(bytes)
    - File contains "P6-E01" or "Phase 7" in comments
    - All 7 core skills appear as rows
    - Values are real measured numbers, not placeholders
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: User verification — all Phase 7 plans executed</name>
  <files></files>

  <read_first>
    - All SUMMARY.md files from Plans 01-05
    - 07-VERIFY.sh full output
    - benchmark-results.tsv
  </read_first>

  <action>
    Final verification checkpoint. Run the full verification suite and present results:

    ```bash
    bash .planning/phases/07-持续优化/07-VERIFY.sh 2>&1
    ```

    Then present a summary of all D-xx items and their status:

    | Decision | Description | Status |
    |----------|-------------|--------|
    | D-03 | Structured registration (triggers/exclude) | Check via VERIFY.sh |
    | D-04 | Proactive intent detection | Check via VERIFY.sh |
    | D-05 | Post-action verification | Check via VERIFY.sh |
    | D-06 | Model preference declaration | Check via VERIFY.sh |
    | D-07 | P6-E01 performance benchmark | benchmark-results.tsv |
    | D-09 | Gap detection | Check via VERIFY.sh |
    | D-10 | GitHub auto-search | skill-discovery.sh |
    | D-11 | Phase 2 threshold filtering | In skill-discovery.sh |
    | D-12 | Tiered grading | In skill-discovery.sh |
    | D-13 | Replacement logic | In skill-discovery.sh |
    | D-14 | Silent auto-record | feedback-state.json counter |
    | D-15 | Feedback prompt every 10 | In scientific-do Step 5 |
    | D-16 | Update check | update-check.sh |
    | D-17 | Manual update approval | In update-check.sh |
    | D-18 | Post-update smoke test | HARD-GATE 4 |
    | D-19 | Extension activation | In scientific-do Step 2 |
    | D-20 | No preset priority | In scientific-do Step 2 |

    Ask user to:
    1. **Review the verification output** — confirm all checks pass
    2. **Review the benchmark results** — confirm measurements look reasonable
    3. **Approve or request changes** to any plan output
  </action>

  <how-to-verify>
    1. Run `bash .planning/phases/07-持续优化/07-VERIFY.sh`
    2. Review the table of D-xx items above
    3. Open `.planning/phases/07-持续优化/benchmark-results.tsv` to review performance data
    4. Type "approved" if all checks pass, or describe issues to fix
  </how-to-verify>

  <resume-signal>Type "approved" or describe specific issues to address</resume-signal>

  <acceptance_criteria>
    - All 17 D-xx decisions have corresponding implementations
    - VERIFY.sh exits 0 with all checks green
    - User confirms acceptance of the Phase 7 output
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gap detection -> feedback-state.json | Gap record written to JSON file — controlled format |
| Gap detection -> skill-discovery.sh trigger | User must confirm before discovery runs — no automatic execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-02 | Input Validation | Gap detection logs user request | accept | Request stored in memory; JSON field is simple string |
| T-7-03 | Spoofing | Gap detection offers discovery | mitigate | Discovery script does NOT auto-install — only displays candidates. User must evaluate before any action |
</threat_model>

<verification>
- [ ] bash .planning/phases/07-持续优化/07-VERIFY.sh exits 0
- [ ] benchmark-results.tsv has real measured values for all 7 skills
- [ ] feedback-state.json has `gaps: []` array
- [ ] scientific-do/SKILL.md has gap detection section
- [ ] User confirmed acceptance at checkpoint
</verification>

<success_criteria>
- Gap detection node added to scientific-do Step 2 routing
- feedback-state.json initialized with gaps array
- benchmark-results.tsv contains real performance measurements for all 7 core skills
- Verification suite validates all D-xx items
- Phase 7 complete — all 17 decisions implemented
</success_criteria>

<output>
After completion, create `.planning/phases/07-持续优化/07-05-SUMMARY.md`
</output>
